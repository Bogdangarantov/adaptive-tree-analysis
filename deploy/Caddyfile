{$APP_DOMAIN} {
    encode zstd gzip

    route {
        @api path /api/*
        reverse_proxy @api app:8080

        @ws path /ws /ws/*
        reverse_proxy @ws app:8080

        root * /srv
        try_files {path} /index.html
        file_server
    }
}
