# 🎓 Дипломна робота
## Веб-платформа для візуалізації та експериментального аналізу самобалансувальних дерев

## Що це за проєкт

`adaptive-tree-analysis` — це платформа для дослідження самобалансувальних дерев пошуку:
- AVL
- Red-Black
- Splay

Проєкт складається з двох окремих модулів:
- `application` — backend (Java + Spring Boot), де виконуються всі алгоритмічні обчислення, кроки операцій і benchmark-метрики.
- `ui` — frontend (Vue + D3.js), який відповідає лише за візуалізацію, анімацію та керування сценаріями програвання.

Ключовий архітектурний принцип:
- алгоритми живуть лише на backend;
- frontend не містить логіки балансування дерева, а лише відображає події/знімки, отримані через REST API.

---

# 📌 Загальна стратегія

Мета: створити веб-систему, яка:
- реалізує AVL, Red-Black та Splay дерева
- візуалізує їх роботу
- дозволяє покрокову анімацію
- проводить експериментальні вимірювання
- збирає статистику

Розробка поділяється на 6 етапів.

---

# 🚀 ЕТАП 1 — Підготовка середовища

## Backend
- [ ] Створити Spring Boot проект
- [ ] Налаштувати REST API
- [ ] Підключити Lombok (опційно)
- [ ] Створити базову структуру пакетів:
  - controller
  - service
  - model
  - algorithm
  - dto

## Frontend
- [ ] Створити React/Vite проект
- [ ] Підключити D3.js
- [ ] Створити базову сторінку з SVG контейнером

---

# 🌳 ЕТАП 2 — Реалізація алгоритмів (Backend)

## 2.1 AVL Tree
- [ ] Node структура
- [ ] Insert
- [ ] Delete
- [ ] Search
- [ ] Ротації (LL, RR, LR, RL)
- [ ] Обчислення висоти
- [ ] Генерація JSON дерева

## 2.2 Red-Black Tree
- [ ] Node з кольором
- [ ] Insert
- [ ] Delete
- [ ] Балансування
- [ ] Перефарбування
- [ ] Ротації

## 2.3 Splay Tree
- [ ] Insert
- [ ] Search (з splaying)
- [ ] Delete
- [ ] Zig / Zig-Zig / Zig-Zag

---

# 🔄 ЕТАП 3 — Покроковий режим

Модифікувати алгоритми так, щоб вони:

- [ ] Записували список кроків (List<Step>)
- [ ] Фіксували:
  - відвідування вузла
  - тип ротації
  - зміну висоти
  - зміну кольору
- [ ] Повернули масив кроків через API

Приклад Step:
--- 
`{
"type": "rotate_left",
"nodeId": 30
}`

# 🎨 ЕТАП 4 — Візуалізація (Frontend)

## 4.1 Базове дерево
- [ ] Використати d3.tree()
- [ ] Побудувати SVG вузли
- [ ] Побудувати лінії-зв’язки
- [ ] Відображати ключі

## 4.2 Динамічне оновлення
- [ ] Реалізувати data binding
- [ ] Використати enter / update / exit
- [ ] Додати transition()

## 4.3 Анімація
- [ ] Плавне переміщення вузлів
- [ ] Підсвічування активного вузла
- [ ] Анімація ротацій
- [ ] Покрокове програвання

---

# 📊 ЕТАП 5 — Модуль експериментів

## 5.1 Генерація тестів
- [ ] Random
- [ ] Sorted
- [ ] Reverse sorted
- [ ] Almost sorted
- [ ] Zipf distribution

## 5.2 Збір метрик
- [ ] Час виконання
- [ ] Кількість ротацій
- [ ] Максимальна висота
- [ ] Середня глибина вузла
- [ ] Кількість перебалансувань

## 5.3 API для запуску тесту
- [ ] POST /benchmark
- [ ] Повернення JSON статистики

---

# 📈 ЕТАП 6 — Графіки та аналітика

- [ ] Побудова графіка "час vs n"
- [ ] Побудова графіка "висота vs n"
- [ ] Побудова графіка "ротації vs n"
- [ ] Порівняльний режим (3 дерева одночасно)
- [ ] Експорт у CSV

---

# 🧠 Наукова частина

- [ ] Формалізувати модель доступу (параметр локальності)
- [ ] Сформулювати гіпотезу
- [ ] Провести серію експериментів
- [ ] Побудувати емпіричні залежності
- [ ] Порівняти з теоретичними межами

---

# 🏁 Фінальний етап

- [ ] Деплой на VPS / Raspberry Pi
- [ ] Написати документацію
- [ ] Підготувати демо для захисту
- [ ] Підготувати презентацію

---

# 🔥 Мінімальний MVP (щоб не перегоріти)

1. AVL дерево
2. Візуалізація вставки
3. Анімація ротації
4. Базовий benchmark

Все інше — поступово.

---

# ⏳ Орієнтовний таймлайн

| Етап | Час |
|------|------|
| Алгоритми | 3–4 тижні |
| API | 1 тиждень |
| D3 базове дерево | 2 тижні |
| Анімація | 2–3 тижні |
| Benchmark | 2 тижні |
| Аналітика | 2 тижні |

---

# 🎯 Головний принцип

Спочатку:
- правильність алгоритмів

Потім:
- анімація

Потім:
- аналітика

Не навпаки.

---

# 🧩 Кінцева мета

Отримати:
- повноцінну веб-платформу
- експериментальні результати
- наукову новизну
- сильну магістерську роботу

---

# CI/CD

У репозиторії додано GitHub Actions pipeline:

- `CI`:
  - запускається на `pull_request` і `push` у `main`/`master`;
  - виконує backend тести в `application` на Java 21 з PostgreSQL service;
  - виконує frontend build у `ui`;
  - перевіряє, що `backend-ui-dto/generated/api-types.ts` синхронізований з OpenAPI контрактом.

- `CD`:
  - запускається на `push` у `main`/`master` або вручну через `workflow_dispatch`;
  - збирає backend JAR і frontend `dist`;
  - публікує обидва результати як GitHub Actions artifacts.

Окремо додано production deploy для VPS:

- [docker-compose.prod.yml](/Users/bohdan/Projects/adaptive-tree-analysis/docker-compose.prod.yml)
- [application/Dockerfile](/Users/bohdan/Projects/adaptive-tree-analysis/application/Dockerfile)
- [deploy/caddy.Dockerfile](/Users/bohdan/Projects/adaptive-tree-analysis/deploy/caddy.Dockerfile)
- [deploy/Caddyfile](/Users/bohdan/Projects/adaptive-tree-analysis/deploy/Caddyfile)
- [deploy-vps.yml](/Users/bohdan/Projects/adaptive-tree-analysis/.github/workflows/deploy-vps.yml)

Production схема:

- `Caddy` автоматично випускає та поновлює HTTPS сертифікати для домену;
- `Caddy` віддає frontend і проксіює `/api/*` та `/ws` на Spring Boot;
- `PostgreSQL` працює як окремий контейнер у тому ж compose stack.
- У production для PostgreSQL не публікуються host ports, тому БД доступна лише всередині docker network.

Що потрібно на VPS:

- Docker Engine + Docker Compose plugin;
- відкриті порти `80`, `443`, `22`;
- домен з `A` записом на IP сервера;
- директорія для застосунку, наприклад `/opt/adaptive-tree-analysis`.

GitHub Secrets для автоматичного deploy:

- `VPS_HOST` — IP або домен VPS;
- `VPS_USER` — SSH користувач;
- `VPS_SSH_KEY` — приватний SSH ключ для деплою;
- `VPS_APP_DIR` — директорія застосунку на сервері;
- `APP_DOMAIN` — публічний домен, наприклад `tree.example.com`;
- `DB_PASSWORD` — довгий випадковий пароль PostgreSQL, мінімум 20 символів, з великими/малими літерами та цифрами.

Перший деплой:

1. Встановити Docker і Docker Compose на VPS.
2. Додати DNS запис домену на IP VPS.
3. Додати secrets у GitHub repository settings.
4. Запустити workflow `Deploy VPS` вручну або зробити push у `main`.

Після цього `Caddy` сам отримає і буде оновлювати TLS-сертифікати, якщо домен уже вказує на сервер і порти `80/443` доступні з інтернету.

Для локального `docker-compose.yml` порт PostgreSQL прив'язаний лише до `127.0.0.1:5433`, тому БД не слухає зовнішній інтерфейс машини.
